huwwp dotfiles for Fedora 38 Sway spin

For waybar icons download fontawesome free and copy the .otf files to `~/.fonts/` and refresh the font cache `fc-cache -f -v`.If icons aren't working go to the fontawesome website and replace them from the free set. 
